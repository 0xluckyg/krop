import React, {Fragment} from 'react'

import { withStyles } from '@material-ui/core/styles';

class AlertEditor extends React.Component {
    constructor(props) {
        super(props)
    }

    render() {
        return (
            <Fragment>
                
            </Fragment>
        )
    }
}

const useStyles = theme => ({    
    
})

export default withStyles(useStyles)(AlertEditor)