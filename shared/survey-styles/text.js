const HEADING = {
    wordWrap: 'break-word'
}
const HEADING_DESKTOP = {
    ...HEADING
}

const SUBHEADING = {
    wordWrap: 'break-word'
}
const SUBHEADING_DESKTOP = {
    ...SUBHEADING
}

const PARAGRAPH = {
    wordWrap: 'break-word'
}
const PARAGRAPH_DESKTOP = {
    ...PARAGRAPH
}

module.exports = {
    HEADING,
    HEADING_DESKTOP,
    SUBHEADING,
    SUBHEADING_DESKTOP,
    PARAGRAPH,
    PARAGRAPH_DESKTOP
}