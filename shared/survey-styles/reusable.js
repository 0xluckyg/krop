const CONTAINER = {
    margin: '30px 0px'
}

const CONTAINER_DESKTOP = {
    ...CONTAINER
}

const TEXT = {
    fontSize: 18
}

const TEXT_DESKTOP = {
    ...TEXT
}

const QUESTION = {
    fontSize: 16,
    margin: '0px 0px 10px 0px',
    opacity: 0.8,
    fontWeight: 700
}

const QUESTION_DESKTOP = {
    ...QUESTION
}

module.exports = {
    CONTAINER,
    CONTAINER_DESKTOP,
    TEXT,
    TEXT_DESKTOP,
    QUESTION,
    QUESTION_DESKTOP
}
